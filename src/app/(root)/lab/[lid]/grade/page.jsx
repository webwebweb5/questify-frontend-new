import { CONFIG } from 'src/config-global';

// ----------------------------------------------------------------------

export const metadata = { title: `Lab grade - ${CONFIG.site.name}` };

export default function LabGradePage() {
  return <div className="">Lab Grade</div>;
}
